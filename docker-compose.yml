version: "3"
services:
  atcoder-myenv:
    build:
      context: .
      dockerfile: ./Dockerfile
      args:
        WORKDIR: /project/src
    volumes:
      - .:/project
      - ${USERPROFILE-~}/.ssh:/home/vscode/.ssh
    container_name: atcoder-myenv
    tty: true
